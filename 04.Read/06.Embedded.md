# Embedded documents and Arrays

- A Document contains key value pairs.
- The value can be another document or an array of items where items can be primitive or document.


## Array of primitives

- Check if this value is in the list or not. There may be other values and that is okay.
- Iterate over all the elements and see if any of the element is having this value.
- Select all documents where in the array, this is one of the value in the list.

## Array of documents

- Query the array field by using the dot notation to go to the document field we want to query for.


## Nested documents

- We can just write the path of nested field from top level using dot notation and then compare it with a value.
