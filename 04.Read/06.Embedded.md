# Embedded documents and Arrays

- A Document contains key value pairs.
- The value can be another document or an array of items where items can be primitive or document.


## Array of primitives

- We can query array field and pass one primitive value.
- All documents which contain this primitive value will be selected.
