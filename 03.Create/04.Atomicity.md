# Atomicity

- A document is either completely inserted or not at all.
- It's never like some fields are stored and others couldn't be because some crash happened.
- With insertMany it doesn't work there. One document of insertMany will fail or succeed.
