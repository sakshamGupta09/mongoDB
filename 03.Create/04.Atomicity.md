# Atomicity

- Per document level. One document is either fully stored or not.
- It's never like some fields are stored and others couldn't be because some crash happened.
- With insertMany it doesn't work there. 
