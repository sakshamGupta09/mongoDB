# Ordered inserts

- When inserting multiple documents, each document is processed standalone. Whole operation is not atomic by default.
- If there is an error in inserting 5th document, the operation will stop but the first 4 documents before error will be inserted.
