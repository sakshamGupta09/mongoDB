# Databases, Collections and Documents

- There is a DB server.
- A server has many databases.
- Each databasse has multiple collections which is equivalent to a table in SQL world.
- Each collection has multiple documents which is a row of the table in SQL world.
