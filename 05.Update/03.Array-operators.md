# Array Operators

- We want to update an array.

## Update matching values in array

```javascript
db.collection.updateOne(
   { <array>: value ... },
   { <update operator>: { "<array>.$" : value } }
)
```
