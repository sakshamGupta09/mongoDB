# Upsert

- Our query did not match any documents. So, now what to update?
- By default it will not update any documents and return `matchedcount` as 0.
- We can instead insert a new document by using information provided in query and how to update object.

```javacsript
db.collection.updateMany({}, {}, { upsert: true })
```
